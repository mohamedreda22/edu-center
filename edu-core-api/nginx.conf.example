server {
    listen 80;
    server_name rakan-api.flowship.site;

    location / {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # SSL configuration (handled by Certbot/Let's Encrypt)
    # listen 443 ssl;
    # ssl_certificate /etc/letsencrypt/live/rakan-api.flowship.site/fullchain.pem;
    # ssl_certificate_key /etc/letsencrypt/live/rakan-api.flowship.site/privkey.pem;
}
